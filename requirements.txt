# Core API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
PyYAML>=6.0
httpx>=0.27.0
python-dotenv>=1.0.0

# RAG / LangChain
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
langchain-huggingface>=0.0.3
faiss-cpu>=1.8.0
pypdf>=4.0.0
sentence-transformers>=2.7.0

# Optional: Firebase auth (production)
firebase-admin>=6.5.0

# Optional: local Ollama fallback
ollama>=0.3.0

# Legacy / unused in deploy path (kept for compatibility)
Pillow>=10.0.0
